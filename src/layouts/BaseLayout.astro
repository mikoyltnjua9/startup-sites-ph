---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Startup Sites PH | Web Development, SEO & Social Media',
  description = 'Startup Sites PH is a web development agency that builds fast, modern websites and grows them with SEO and social media marketing.',
} = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title}</title>
    <script is:inline>
      // Runs before first paint so the correct theme applies immediately —
      // waiting for the main bundle (a deferred module script) would show
      // a flash of the wrong theme first.
      (function () {
        try {
          var stored = localStorage.getItem('theme');
          var theme =
            stored === 'dark' || stored === 'light'
              ? stored
              : window.matchMedia('(prefers-color-scheme: dark)').matches
                ? 'dark'
                : 'light';
          document.documentElement.setAttribute('data-theme', theme);
        } catch (e) {
          // Storage/matchMedia unavailable — fall back to the default
          // (light) tokens already defined in :root.
        }
      })();
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
